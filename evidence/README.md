# Evidence

Machine-local proof output is written to ignored `evidence/local/`. Reviewed, redacted parser fixtures may be promoted to `evidence/fixtures/` only after confirming that paths, environment values, credentials, source, and raw session identifiers are absent.

Milestone 2 must eventually produce a versioned redacted bundle with CLI versions, normalized auth states, stage trace, hashed sessions, Git checkpoints, policy decisions, validation, findings, and explicit prohibited-action evidence. The bundle ends in `AWAITING_MAINTAINER_DECISION`; it is not a `GO`.


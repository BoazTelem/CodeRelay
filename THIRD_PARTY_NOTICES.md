# Third-Party Notices

Production and development dependencies are declared in `package.json` and locked in `package-lock.json`. Generate the machine-readable CycloneDX inventory with `npm run sbom` and review the dependency license data before any release.

CodeRelay does not redistribute Codex or Claude Code. Users install and authenticate those official tools separately, subject to their providers' terms.

